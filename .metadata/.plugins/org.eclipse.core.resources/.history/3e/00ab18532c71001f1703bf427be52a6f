/*
 * package com.example.tronics.model;
 * 
 * import java.time.LocalDateTime;
 * 
 * import jakarta.persistence.Column; import jakarta.persistence.Entity; import
 * jakarta.persistence.GeneratedValue; import
 * jakarta.persistence.GenerationType; import jakarta.persistence.Id; import
 * jakarta.persistence.Table;
 * 
 * @Entity
 * 
 * @Table(name = "inventory") public class Inventory {
 * 
 * @Id
 * 
 * @GeneratedValue(strategy = GenerationType.IDENTITY) private Long id;
 * 
 * @Column(name = "product_id", nullable = false) private Long productId;
 * 
 * @Column(nullable = false) private int quantity;
 * 
 * @Column(name = "location") private String location; // Optional
 * 
 * @Column(name = "last_updated", nullable = false) private LocalDateTime
 * lastUpdated;
 * 
 * // Constructors, Getters, and Setters public Inventory() { }
 * 
 * public Inventory(Long productId, int quantity, String location, LocalDateTime
 * lastUpdated) { this.productId = productId; this.quantity = quantity;
 * this.location = location; this.lastUpdated = lastUpdated; }
 * 
 * public Long getId() { return id; }
 * 
 * public void setId(Long id) { this.id = id; }
 * 
 * public Long getProductId() { return productId; }
 * 
 * public void setProductId(Long productId) { this.productId = productId; }
 * 
 * public int getQuantity() { return quantity; }
 * 
 * public void setQuantity(int quantity) { this.quantity = quantity; }
 * 
 * public String getLocation() { return location; }
 * 
 * public void setLocation(String location) { this.location = location; }
 * 
 * public LocalDateTime getLastUpdated() { return lastUpdated; }
 * 
 * public void setLastUpdated(LocalDateTime lastUpdated) { this.lastUpdated =
 * lastUpdated; } }
 */